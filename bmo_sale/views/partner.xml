<?xml version="1.0"?>
<odoo>

    <record id="view_partner_form_inherit_bmo_sales" model="ir.ui.view">
        <field name="name">res.partner.form.inherit.bmo_sales</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">

            <xpath expr="//field[@name='category_id']" position="after">
                <field name="saldo_active"/>
                <field name="saldo_limit" invisible="saldo_active != True"/>
                <field name="ar_value" invisible="saldo_active != True"/>
                <field name="saldo_sisa" invisible="saldo_active != True"/>
            </xpath>
            <xpath expr="//field[@name='user_id']" position="replace">
                <field name="user_id" widget="many2one_avatar_user" invisible="1"/>
                <field name="employee_id" widget="many2one_avatar_user"/>
            </xpath>
        </field>
    </record>

</odoo>
