<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record id="sale_order_tree_inherit_bmo_sale_approval_route" model="ir.ui.view">
            <field name="name">sale.order.list.form.inherit.bmo_sale_approval_route</field>
            <field name="model">sale.order</field>
            <field name="inherit_id" ref="sale.sale_order_tree"/>
            <field name="arch" type="xml">
                <xpath expr="//field[@name='state']" position="after">
                    <field name="is_current_approver" optional="show"/>
                    <field name="next_approver" optional="show"/>
                    <field name="so_status" optional="show"/>
                </xpath>
            </field>
        </record>

        <record id="purchase_order_approval_form_inherit_bmo_sale_approval_route" model="ir.ui.view">
            <field name="name">sale.order.approval.form.inherit.bmo_sale_approval_route</field>
            <field name="model">sale.order</field>
            <field name="inherit_id" ref="sale.view_order_form"/>
            <field name="arch" type="xml">
                <xpath expr="//button[@name='action_cancel']" position="attributes">
                    <attribute name="invisible">state not in ['draft', 'sent', 'to_approve', 'sale'] or not id or locked</attribute>
                </xpath>
                <xpath expr="//button[@name='action_cancel']" position="before">
                    <button name="button_approve" type="object" invisible="is_current_approver == False" string="Approve Order" class="oe_highlight" data-hotkey="z"/>
                </xpath>

                <xpath expr="//button[@name='action_quotation_send'][1]" position="attributes">
                    <attribute name="invisible">state not in ['draft', 'sent']</attribute>
                </xpath>
                <xpath expr="//button[@name='action_quotation_send'][2]" position="attributes">
                    <attribute name="invisible">1</attribute>
                </xpath>
                <xpath expr="//button[@name='action_quotation_send'][3]" position="attributes">
                    <attribute name="invisible">1</attribute>
                </xpath>
                <xpath expr="//button[@name='action_quotation_send'][4]" position="attributes">
                    <attribute name="invisible">1</attribute>
                </xpath>

                <xpath expr="//field[@name='payment_term_id']" position="after">
                    <field name="sale_team_id" options='{"no_edit": True, "no_create": True, "no_open": True}' 
                        invisible="sale_order_approval_route == 'no'" required="sale_order_approval_route == 'required'"/>
                </xpath>

                <notebook position="inside">
                    <page name="approval_route" string="Approval Route" invisible="sale_team_id == False">
                        <field name="approver_ids">
                            <list>
                                <field name="user_id"/>
                                <field name="role"/>
                                <field name="state"/>
                            </list>
                        </field>
                        <group>
                            <field name="sale_order_approval_route" invisible="True"/>
                            <field name="is_current_approver" invisible="True"/>
                            <field name="next_approver"/>
                        </group>
                    </page>
                </notebook>
            </field>
        </record>

    </data>
</odoo>
